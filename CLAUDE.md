# エディトリアルポスター・ジェネレーター

## プロジェクトの目的
写真1枚を入力すると、「上半分=元写真（軽い色調補正）、下半分=紙の上に小さく配置した手描き風イラスト＋タイポグラフィ」という構成のポスター画像を、**生成AIを一切使わず**古典的な画像処理（OpenCV / rembg / Haar Cascade）だけで自動生成するパイプライン。

**AI不使用は確定方針。** 理由：アウトプットのタッチを毎回一定に保つため／API従量課金を避けるため／即時変換（数秒で結果が欲しい）という要件のため。この前提を勝手に変えないこと。

## 現在の完成度
- 正面を向いた顔がある写真（1〜2人）は実用レベルに近い
- 後ろ姿・横顔・手元アップなど顔が写っていない構図は、顔の意味理解ができないため品質が大きく落ちる（未解決）
- 柄物の服はある程度緩和したが完璧ではない

## ファイル構成と役割
- `poster_pipeline.py` — 基盤パイプライン：rembgによる被写体セグメンテーション、背景の取捨選択ロジック（`mode="objects"`推奨、`mode="ellipse"`は検証の結果不採用）、mean-shift＋k-means色抽出、Tombowパレットへのスナップ、紙テクスチャ生成
- `tombow_palette.py` — Tombow ABT 16色マスターパレット。HEX値は`reference/tombow_color_chart.png`（ユーザー提供の公式カラーチャート）からピクセル実測した値。**この値は近似ではなく実測**なので、勝手に上書きしないこと
- `landmark_face.py` — Haar Cascade（`cv2.data.haarcascades`、ダウンロード不要）による顔検出。顔ボックス内は色クラスタリングに頼らず、肌色フラット塗り＋目・眉・口を直接描画する。**MediaPipeは使用不可**（モデルファイルのDLがネットワーク制限でブロックされる。storage.googleapis.com等の外部モデルDLは全般的に不可能な環境だと想定すること）
- `flat_style.py` — 現在のメイン実装。「フラット・ビビッド」スタイル（質感ノイズなし・彩度ブースト・色のまだら統合・線の太さに強弱）。`build_flat_style()`が3:4版、`build_flat_style_a4()`がA4比率(1:1.414)のフル版
- `paint_by_number.py` — 色番号付きペイント・バイ・ナンバーテンプレート生成。**古い「ドライブラシ」スタイル版がベースのまま**で、`flat_style.py`のロジックへの移植が未着手

## 過去に試して失敗したこと（同じ轍を踏まないための記録）
- **GrabCut**での被写体切り抜き → 矩形初期化だけでは顔の一部を誤って背景判定するなど不安定。rembgに置き換えて解決済み
- **エッジ密度だけ**で背景の残す/残さないを判定 → 木目や繰り返しテクスチャを誤って「残す」判定してしまう。局所色コントラスト（小窓平均と広い近傍平均の差）を主指標にして解決済み
- **楕円＋ランドマーク例外**での背景クロップ（ユーザー発案） → 被写体周辺の落ち葉・苔・日よけの縁など意味のないテクスチャまで機械的に拾ってしまい、現行の「物体選別」ロジックより不安定と判明。不採用
- **顔検出のHaar Cascadeしきい値（minNeighbors）を厳しくしすぎる** → 誤検出は減るが、本物の顔も検出できなくなる（プールの写真で顔が消えた）。`minNeighbors=3, minSize=(30,30)`が現在の最適値。背景人物の誤検出は`keep_mask`との重なり率（50%未満は無視）でフィルタする方式の方が有効
- **肌色をTombow全16色から最近傍探索** → 日陰の肌はLAB色空間でグレー系の色の方が数学的に近くなることがあり、顔がグレーになるバグが起きた。肌色候補を肌色系10色に限定して解決済み
- **顔ボックス全体の平均で肌色をサンプリング** → 前髪・帽子の影・輪郭外まで含んでしまい不自然な色になる。頬〜鼻筋の中央下エリアだけ、かつ明暗の上下25%を除いた中央値を使う方式に変更して解決済み
- **柄物の服をbilateralFilterで平滑化** → エッジ保存型フィルタなので、強いコントラストの柄はほぼ素通りしてしまい効果が薄い。局所分散が高いエリアだけ強めのGaussianBlurで潰す方式に変更して効果あり

## 次にやると良いこと（優先順位順）
1. **Tombow実物ペンでの最終キャリブレーション** — チャート画像からの実測値はできているが、実物のペンで紙に塗って撮影した写真があれば、印刷・モニター経由の誤差もなくせる。ユーザーがペンを入手次第、写真が送られてくる可能性がある
2. ~~横顔・後ろ姿への対応~~ **完了**（`landmark_face.py`の`detect_faces()`）。`haarcascade_profileface.xml`を左右反転の2方向で使い、正面顔が見つからない場合のフォールバックにした。あわせて複数人写真でrembgが一部の人物を無視する問題にも`recover_missed_people()`で対応済み。詳細はCHANGELOG.md参照
3. **柄物の服の扱いをさらに詰める** — 花柄シャツなど元々情報量が多い服は、いっそ単色化して柄は諦める、という割り切りの判断も検討。実写真（ワンちゃんのライン画柄シャツ等）でまだらな色分割が残るケースを確認済み
4. **`paint_by_number.py`を`flat_style.py`のロジックに移植** — 現状は旧スタイルのまま
5. **複数枚の一括処理**（フォルダ単位で自動変換）
6. **プロッタ出力（ベクター化）** — 将来的に実際の紙にペン描きさせる場合、輪郭をSVG等のパスとして書き出す工程が別途必要
7. **rembgが人物と手前の大きな小道具（装飾看板・カットアウト等）を1つの塊として認識する問題** — 複数の実写真で確認済み。人物のみを取り出す安全な方法が未確立

## 作業の進め方
- 変更を加えたら、必ず複数枚（正面顔・横顔なし・柄物あり、最低3パターン）でテストしてから「改善した」と判断すること。1枚だけの確認で判断して後から他の写真で壊れていたことが何度もあった
- 進捗・試したこと・ダメだった理由は`CHANGELOG.md`に追記していくこと（このファイルと同じディレクトリに用意）
