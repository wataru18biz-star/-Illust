# CHANGELOG

## 2026-09-18〜19 セッション（Claude via chat での開発）

### やったこと
- rembgによる被写体セグメンテーションの確立
- 背景の取捨選択ロジック確立（`mode="objects"`：面積上位＋内部エッジ密度が低い物体のみ最大3個まで残す）
- Tombow ABT 16色マスターパレット確立、公式カラーチャート画像からHEX値を実測
- Haar Cascadeによる顔検出＋「意味のある形」としての顔描画（`landmark_face.py`）
- 「フラット・ビビッド」スタイル確立（`flat_style.py`）：質感ノイズ除去・彩度ブースト・色のまだら統合・線の太さの強弱
- 柄物の服の簡略化処理を追加（局所分散ベースの検出＋強めのGaussianBlur）
- A4比率(1:1.414)のフルポスター版を追加（`build_flat_style_a4`）

### 未解決のまま持ち越し
- 顔が写っていない構図（後ろ姿・手元アップ）の品質が低い
- `paint_by_number.py`が旧スタイルのまま、`flat_style.py`への移植が未着手
- 複数枚の一括処理は未実装
- Tombowの色は公式チャート画像からの実測値。実物のペンでの最終キャリブレーションはまだ

### 次のセッションへ
このファイルの続きに追記していくこと。「試したが効果がなかった/悪化した」ケースも必ず記録し、同じ変更を繰り返さないようにする。

## 2026-09-19 セッション（CLAUDE.md優先順位2番「横顔・後ろ姿への対応」）

### 環境まわりの発見（作業開始時）
- このサンドボックス環境にはcv2/numpy/rembg等が未インストールで、pipで最新版を入れたところ
  `opencv-python-headless` 5.0.0.93 が入り、**`cv2.data.haarcascades`のディレクトリにXMLファイルが
  1つも同梱されていない**ことが判明（`CascadeClassifier`はエラーなく読み込めるが、
  `detectMultiScale`が常に0件を返す静かな壊れ方をする）。`landmark_face.py`のコメントにある
  「ダウンロード不要」という前提が、opencv-pythonの新しいメジャーバージョンでは崩れている。
  → `opencv-python-headless==4.10.0.84`に固定すれば`haarcascade_profileface.xml`含め全ファイルが
  同梱されていることを確認。`requirements.txt`を新規作成してピン留めした。
- リポジトリに元々requirements.txtが存在せず、テスト用画像も同梱されていなかった。
  このサンドボックスはネットワーク越しに実在人物の写真を取得できない（ユーザー提供の
  `/mnt/user-data/uploads/*.jpeg`もこの環境には無い）ため、`skimage.data`に同梱されている
  ローカル完結の実写真（`astronaut`＝正面顔ポートレート、`camera`＝カメラマンの横顔）を使い、
  さらに`astronaut`の胴体部分に自作のチェッカー柄を合成した画像を作って
  「正面顔・横顔（正面検出に失敗するケース）・柄物」の3パターンとした。
  実際のユーザー写真（特に完全な後ろ姿・手元アップ）での検証はまだ済んでいないため、
  次にユーザー写真が手に入ったタイミングで必ず再確認すること。

### やったこと
- `landmark_face.py`に`profile_cascade`（`haarcascade_profileface.xml`）を追加。
- `detect_faces()`を新設：正面顔をまず検出し、`keep_mask`と重ならない/見つからない場合に
  横顔検出をフォールバックとして試す。`haarcascade_profileface.xml`は一方向（検出ボックスの
  鼻先側が右寄りになる向き）にしか反応しないため、画像を左右反転してもう一度検出し、
  座標を元画像系に戻すことでもう片方の向きも拾う（`facing='right'/'left'`）。
  正面検出と重複する領域（IoU的な重なり30%超）は横顔側を捨てて二重描画を防止。
- `draw_landmark_face_profile()`を新設：正面顔用の左右対称な描画（目2つ・口の弧）をそのまま
  横顔に使うと不自然になるため、目1つ・眉1本・鼻(輪郭の外に少しはみ出す鉤形の線)・
  頬の陰影1箇所・口は前方寄りの短い線、という非対称な簡易パーツ構成にした。
  肌色サンプリングは正面版と同じ「明暗上下25%を除いた中央値」方式を踏襲。
- `flat_style.py`（現行メイン実装）・`landmark_face.py`（旧実装）の両方の顔検出箇所を
  `detect_faces()`経由に差し替え。`paint_by_number.py`はまだ顔検出を使っていないため未変更
  （優先順位4番の移植待ち）。

### テスト結果（3パターン、`flat_style.build_flat_style`で確認）
- 正面顔（astronaut）：変更前と完全に同一の出力（回帰なし）。
- 横顔／正面検出失敗（camera）：**変更前は顔ボックス内が色クラスタリングのままの
  まだら模様（目鼻の判別不能）だったのが、変更後はフラットな肌色＋目・眉・鼻・口の
  簡易パーツで顔だとわかる見た目になった。** これが今回のメインの改善点。
- 柄物（チェッカー柄を合成したastronaut）：顔検出まわりは正面顔のまま変化なし（回帰なし）。
  ただし胴体のチェッカー柄部分が細かい断片状に崩れる問題を確認したが、これは変更前の
  ベースライン出力でも同一に発生しており、今回の変更が原因ではない
  （優先順位3番「柄物の服」側の既知の課題として持ち越し。自作の高コントラストな
  チェッカー柄は実際の花柄等より過酷なテストケースになっている可能性が高い点にも注意）。
- `landmark_face.py`（旧実装）側でも同じ3パターンを実行し、クラッシュなし・横顔で
  同様の改善を確認。

### 未解決のまま持ち越し
- 完全な後ろ姿（後頭部のみ・顔のパーツが画像内に一切ない構図）は、正面・横顔どちらの
  Haar Cascadeでも検出しようがないため、今回の変更では改善しない
  （CLAUDE.mdの「AI不使用」方針上、意味理解によるフォールバックは範囲外）。
- 横顔判定はプロフィールカスケードの1方向検出＋左右反転の力技なので、斜め45度程度の
  半横顔（正面でも横顔でもない中間角度）での挙動は未検証。実写真での確認が必要。
- 柄物の服のさらなる調整（優先順位3番）は未着手のまま。
- `paint_by_number.py`への`flat_style.py`ロジック移植（優先順位4番）は未着手。

## 2026-09-19 セッション（ユーザー提供の実写真5枚での検証イテレーション）

ユーザーから実際の家族写真5枚（正面顔＋柄物シャツの男の子、後ろ姿クローズアップ2種、
犬単体、複数の子供＋大人が写る複雑な集合写真）を受け取り、優先順位2番の変更を実写真で
検証。**写真そのものはこのリポジトリにはコミットしていない**（個人が特定できる子供の
写真のため、作業用ディレクトリのみで使用）。

### 発見・修正1：耳を正面顔と誤検出するバグ（修正済み）
- 後ろ姿クローズアップ写真2枚で、後頭部やうなじ付近に目鼻のあるパーツが不自然に
  浮かんで見える出力になっていた。原因を調査したところ、`haarcascade_frontalface_default`
  が**耳を顔として誤検出**していた（`keep_mask`との重なりも100%あるため既存のフィルタでは
  弾けない）。該当ボックスをクロップして目視確認済み。
- 対策として試したが効かなかったもの：
  - eye_cascadeで「目が1つ以上検出できるか」で判定 → 耳の領域でも目カスケードが
    誤って1個反応してしまい、逆に本物の（変顔で目が細い）顔の方が0個だったため無意味。
  - 肌色ピクセルの割合で判定 → 耳は肌色の割合がほぼ100%に近く、髪の影や表情の
    陰影がある本物の顔よりもむしろ高くなるため逆効果。
  - 左右対称性スコア（左半分と右半分の反転を相関）で判定 → 耳の誤検出・本物の顔・
    参考用の正面ポートレート(astronaut)の3ケースがほぼ同じスコア(0.49〜0.51)になり
    判別不能。
  - → **効いた対策**：`haarcascade_frontalface_alt2`（`default`とは別の学習データの
    カスケード）を追加し、両方のカスケードが近い位置（IoU的な重なり30%超）で
    検出した場合のみ「本物の顔」として採用する方式にした。実写真4パターンで
    確認したところ、耳の誤検出は`default`だけが反応し`alt2`は画像全体で0件、
    逆に本物の顔は両方でほぼ同じ位置に検出されており、綺麗に切り分けられた。
    `landmark_face.py`の`detect_faces()`に実装。
- 修正後、後ろ姿クローズアップ2枚とも顔パーツの誤描画が消え、素直な後頭部の
  シルエットになることを確認。正面顔（実写真の男の子・astronaut合成テスト）・
  横顔（cameraman合成テスト）は引き続き正しく検出されることも再確認（回帰なし）。

### 発見・修正2：複数人が写る複雑な構図でrembgが主要被写体を見失う（未修正・要優先度判断）
- 大人・子供合わせて7〜8人が写る集合写真で、出力が意味不明な縦長の断片1本だけになる
  重大なバグを発見。原因を`get_masks()`内の`rembg`出力（アルファチャンネル）まで遡って
  調査したところ、**rembg(u2net)が「手前で座っている2人の子供」ではなく、右端で
  半分だけ枠に入っている別の1人だけを"唯一の顕著物体"として選び、他は全員アルファ値
  ほぼ0(背景)として扱っていた**ことが判明（アルファの閾値を10まで下げても被写体面積は
  7.9%のまま変化せず、閾値の問題ではなくu2net自体の顕著性判定の限界）。
- これはCLAUDE.mdに既知の課題として書かれている「顔の意味理解ができない後ろ姿・手元
  アップ」とは別種の弱点で、**「主要被写体が1人/1グループに収まらない複雑な集合写真」
  全般でrembgの単一顕著物体前提が壊れる**という新しい既知の課題。
- 対策の方向性はあるが（例：`cv2.HOGDescriptor`の人物検出器等、非生成AIの古典的な
  人物検出で複数人のバウンディングボックスを取り、その和集合をkeep_maskに強制的に
  含める、など）、今回は原因特定のみに留め、実装・検証は次回以降に持ち越す
  （30分の検証タイムボックス内で「原因不明のまま場当たり的に直す」よりも、
  「原因を特定して記録する」方を優先した）。単体の犬の写真・1〜2人の写真では
  この問題は発生しないことも確認済み。

### 気づいたが今回は様子見にしたもの
- 正面顔＋柄物シャツの写真で、変顔で頬が紅潮している男の子の肌色が実際より
  かなり濃い茶色（Tombow "879 Brown"）に寄っていた。顔パーツ自体（目・眉・鼻・口・
  輪郭線）は正しく描画されており致命的ではないが、肌色サンプリング（明暗中央値方式）
  が頬の紅潮・影を拾いやすい既知の癖の延長線上の問題。優先度は低いため今回は未対応。

## 2026-09-19 セッション（実写真バッチ2：5枚追加検証、コード変更なし）

ユーザーから追加で実写真5枚（帽子＋ボトルの超クローズアップ、ペダルカー2台に
乗る子供2人、後ろ姿＋花柄シャツ、車単体、ドーナツ装飾の前で父子2人）を受け取り検証。
今回は**コードは変更していない**（原因特定はできたが、安全に直せる範囲を超えると
判断したものが中心）。バッチ1で直した耳の誤検出修正は、別ポーズ（後ろ姿＋花柄シャツ）
でも誤検出を起こしていないことを再確認できた。

### 前回発見した「rembgの単一顕著物体前提」問題が、追加で2パターン見つかった
バッチ1で見つけた「複数人の集合写真で主要被写体を1人しか拾わない」問題と同じ根本原因
（rembg/u2netが「画面内で最も顕著な塊は1つ」という前提で動いている）が、方向違いの
形で2回再現した。**同じ根本原因を持つ既知の課題として蓄積しており、そろそろ優先的に
対策を検討すべき段階だと思われる：**

1. **人を取りこぼす方向**：ペダルカーに乗る子供2人の写真で、手前の1人だけが被写体として
   拾われ、奥のもう1人（白い車、フレーム端で一部見切れている）が完全に無視され、
   出力に一切描かれなかった。バッチ1の集合写真（7〜8人）と同じ現象が、わずか2人の
   写真でも起きることを確認。
2. **逆に余計な物まで拾う方向（新パターン）**：父子2人がドーナツの装飾（大きな
   段ボール製カットアウト）の前で撮った写真で、`detect_faces()`自体は父・子供
   両方の本物の顔を正しく検出できていた（切り出して目視確認済み）。ところが
   `subject_mask`（rembgの出力）を可視化すると、**人物と手前のドーナツの装飾が
   画面奥から地続きの1つの塊として認識されており**、keep_maskが画面のほぼ全体
   （バウンディングボックスがY:345-937, X:32-745＝ほぼ全域）を覆ってしまっていた。
   その結果、紙の上に配置されるはずの小さなイラストが巨大かつ雑然とした塊になり、
   さらにそのkeep_mask内に収まってしまったドーナツ柄の一部を横顔カスケードが
   誤検出（ドーナツの絵柄がたまたま横顔っぽいエッジパターンに合致）していた。
   既存の「keep_maskと50%以上重なる検出だけ採用」フィルタは、そもそもkeep_mask
   自体が広すぎるこのケースでは効かなかった。

対策の方向性（例：`cv2.HOGDescriptor`の人物検出器などAI不使用の古典的手法で人物の
バウンディングボックスを別途取得し、keep_maskを「人物ボックスの和集合」に強制的に
制限する／逆に拡張する、など）はあるが、CLAUDE.mdに記録されている過去の教訓
（GrabCutでの不安定さからrembgに置き換えた経緯）を踏まえると、被写体マスクの
仕組みに手を入れるのはそれなりの規模の変更になり、幅広い写真での再検証が必須になる。
今回の30分〜1時間規模の検証サイクルでは「原因を特定して記録する」までに留め、
実装はユーザーの意向を確認してから着手する。

### 発見・原因特定できたが対策未着手のもの
- 帽子＋ボトルの超クローズアップ写真で顔が検出できなかった。原因は
  ①顔が極端に大きく・傾いており、②柄物の帽子のつばが額に大きくかぶさり、
  ③子供が目を細めて横目で見ている、という3条件の重なりで、frontalカスケードが
  `minNeighbors=1`まで緩めてようやく反応するが、その検出ボックス自体が顔ではなく
  帽子の柄をメインに捉えた誤ったものだった（切り出して目視確認済み）。
  `minNeighbors`を下げるとCLAUDE.mdの過去の教訓通り誤検出が増える危険があるため
  今回は見送り、既知の「傾いた顔・極端なクローズアップに弱い」という課題の
  具体例として記録するに留めた。

## 2026-09-19 セッション（rembgの「単一顕著物体」問題への本格対応）

これまで3回発見した「rembgが複数人のうち一部を無視する」問題について、ユーザーの
了承を得て本格的に対策を実装。`landmark_face.py`に`recover_missed_people()`を新設し、
`flat_style.py`（メイン実装）・`landmark_face.py`（旧実装）の両方の`get_masks()`直後に
組み込んだ。

### 誤検出を判別するフィルタを4種類試したが、どれも実写真では安定しなかった
「本物の人物の顔」と「顔っぽく見えるが実は違うもの（Tシャツの文字柄、装飾グラフィックの
一部）」を判別するための追加フィルタを4種類、これまで発見した実例（耳の誤検出、
Tシャツの文字柄の誤検出、ドーナツ装飾の誤検出、本物の顔4件）で比較検証した：
- 目検出（eye_cascadeで1つ以上目が見つかるか）→ 誤検出側で目が見つかる/本物側で
  見つからないケースが両方あり、ケースバイケースで逆転する。
- 肌色ピクセルの割合 → 誤検出(0.63〜0.97)と本物(0.63〜0.76)の値域が広く重なり合う。
- エッジ密度（Cannyエッジの割合）→ 同一写真内でのペア比較ではうまく分離できても、
  写真が変わると本物の顔の方が誤検出より高エッジ密度になるケースが出て逆転する
  （写真ごとの明るさ・コントラスト・JPEG圧縮の違いに支配されてしまう）。
- 左右対称性（左右反転した相関）→ 誤検出・本物・参考ポートレートがほぼ同じスコア
  (0.49〜0.51)になり判別不能。
どの指標も「その写真の中でだけ」通用する特徴であり、複数の写真をまたいで安定する
閾値が見つからなかった。これは実質的に「学習済み分類器なしでHaar cascadeの
誤検出を完全に除去するのは困難」という、この種の古典的手法が抱える限界に近い。

### 出した結論：多少の誤爆を許容し、人物を消さない方を優先する
「本物の人物がまるごと消える」実害の方が「文字柄などをうっかり人物として復元して
しまう」実害より大きいと判断し、`detect_faces()`が返す検出（正面=default+alt2の
合議制、横顔=左右反転の2方向検出）をそのまま信頼し、keep_maskとの重なりが40%未満の
場合はその顔を中心にした楕円形の「人物がいるはずの範囲」を強制的にkeep_mask/
subject_maskへ追加する方式にした。誤爆時の被害を抑えるため、追加する範囲は
検出ボックスのサイズに比例した控えめな大きさ（顔の横幅の1.3倍・顔の高さの
おおよそ4倍程度の縦長楕円）に留めている。

### テスト結果（既存写真13枚全部で再テスト、コード変更前と比較）
- **完全に改善**：ペダルカー2台の写真で、奥に隠れていたもう1人の子供が復元され、
  2人とも描かれるようになった（バッチ2で発見した具体的な失敗例が解消）。
- **部分的に改善**：7〜8人の集合写真で、以前は縦長の断片1本だけだった出力が、
  2つの人物らしい塊が見える状態まで回復した。ただし、そのうち1つには
  既知の誤検出（Tシャツの文字柄）が紛れ込んで顔が2つ重なったような見た目になっており、
  完璧な仕上がりではない。「意味不明な断片1本」よりは実用上マシ、という位置づけ。
  複数人が密集し互いに重なり合う集合写真は根本的に難しいままで、優先順位の
  高い今後の課題として残る。

## 2026-09-19 セッション（実写真バッチ3：5枚追加検証、コード変更なし）

ユーザーから追加で実写真5枚（10人規模の複雑な集合写真、父子＋段ボール製の
装飾看板、変顔の子供＋困り顔の父＋第三者の耳が写る超クローズアップ、遊具に
乗る子供2人、車のおもちゃに乗る女の子＋しゃがむ父）を受け取り検証。
今回は**コードは変更していない**（見つかった問題はいずれも既知の限界の
再現だったため）。

### 良かった点：`recover_missed_people()`が3例目の複雑な集合写真でも機能した
10人規模の集合写真で、`detect_faces()`が本物の顔を4件検出し、そのうち2件は
すでにkeep_maskに含まれていたが、残り2件（coverage=0.0）が正しく復元された。
結果として「断片1本」ではなく3〜4人分の人物らしい塊が見える出力になった。
バッチ1・2で作った修正が、初見の別写真でも同じように効くことを確認できた
（過学習していない）。

### 新規バグではなく、既知の限界の再現だったもの
- **父子＋段ボール看板の写真**：`subject_mask`を確認したところ、父・子供と
  手前の「Get Curious!」の段ボール製看板・モンキーキャラのカットアウトが
  1つの塊（画面の22%、Y:415-868, X:136-613）として認識されていた。
  ドーナツ装飾の写真で見つけたのと全く同じ「顕著な前景の塊に、人物と装飾物が
  地続きで混ざる」パターン。対策は今回も見送り（同じ理由：プロップが人物の
  体と空間的に重なっており、顔を中心にしたボックスで安全に切り分けられない）。
- **車のおもちゃに乗る女の子＋しゃがむ父の写真**：父親が出力から完全に消えていた。
  原因を調査したところ、`detect_faces()`が父親の顔を正面・横顔どちらでも
  一切検出できていなかった（女の子の顔1件のみ検出）。父親は頭を傾けて
  下を向き気味の斜め向き（正面でも横顔でもない中間角度）で写っており、
  これは横顔対応を実装した際にも記録した「Haar Cascadeは斜め45度前後の
  半横顔に弱い」という既知の弱点そのもの。rembgもこの父親を顕著物体として
  拾っておらず、顔検出・被写体マスクの両方が同時に外れたため、
  `recover_missed_people()`（顔が見つかった場合のみ発火する設計）も
  対応できなかった。

### 個別には問題なかったもの
- 変顔クローズアップ写真（3人の顔が異なる距離・ボケ具合で写る難しい構図）は、
  手前2人の顔が判別可能なレベルで描画されており、破綻はなかった。
- 遊具に乗る子供2人の写真は、2人とも頭部が描かれており、1人は顔パーツ込みで
  良好、もう1人はシルエットのみだが破綻はしていない。特に修正の必要なし。

## 2026-09-19〜20 セッション（実写真バッチ5＋未検証パイプラインの横展開、コード変更なし）

ユーザーから「3時間くらい色んなパターンで検証して」という依頼。文字通り3時間
張り付いて待つことはできないため、代わりに①実写真5枚の検証と②これまで
このセッションで一度も動かしていなかった`build_flat_style_a4`（A4フル版）・
`landmark_face.py`（旧実装）・`paint_by_number.py`（ペイントバイナンバー）の
横展開検証、の2方向で「色んなパターン」をカバーした。**いずれもコード変更なし**
（見つかったものは全て既知の傾向の再確認、または対象外のエッジケース）。

### 実写真5枚（麺を食べる超クローズアップ、トーマスの乗り物、いちごを持つ男の子、
### キャンプ地の犬、YouTube動画を映したノートPC画面を撮った写真）
- 全て致命的な破綻なし。いちごの写真はこのバッチで一番綺麗な結果（顔パーツ良好）。
- トーマスの乗り物の写真で`recover_missed_people()`が発火。復元された領域を
  確認したところ、トーマスの顔（乗り物自体に描かれた絵）ではなく、**背景の
  看板（赤い駅名標のような看板）**を横顔と誤検出したものだった。想定内の
  「多少の誤爆を許容する」トレードオフの再確認。なお`landmark_face.py`（旧実装）
  で同じ写真を処理したところ、トーマスの乗り物自体に描かれた顔がちゃんと
  検出・スタイライズされており（乗り物に本当に顔が描いてあるので、結果的に
  「正しい」検出）、これはこれで面白い副産物だった。
- 麺を食べる超クローズアップ写真は、顔全体がフレームいっぱいに斜めに写り、
  口を開けて麺を頬張っているという難しい構図。`detect_faces()`は正面ではなく
  横顔（'left'）として検出しており、非対称な横顔用パーツ描画と実際の表情
  （大きく開いた口）がかみ合わず、見た目が崩れた。既知の「傾いた顔・極端な
  クローズアップ・通常と異なる表情に弱い」という課題の具体例として記録するに
  留めた（`minNeighbors`調整は過去の教訓により見送り）。
- ノートPC画面を撮った写真（YouTube動画に本物の人物の顔が映っている）は、
  このプロジェクトが想定する「人物・ペットの実写真」とは異なるメタな入力
  （写真の中の画面に写真が映っている）。`subject_mask`がモニターの筐体と
  画面内の顔を別々の塊として認識し、出力ではモニターの黒い四角と、独立して
  浮かぶ顔のパーツが分離して見える結果になった。プロジェクトの想定用途外の
  ため、修正対象にはしないことにした（記録のみ）。

### 未検証だった3つのパイプラインの動作確認
- **`build_flat_style_a4`**（A4フル版、これまで一度もテストしていなかった）：
  いちごの写真・トーマスの乗り物の写真の両方でクラッシュなく正常に動作。
  上半分の色調補正済み写真＋下半分の小さいイラストという構成が意図通り
  出力されることを確認。
- **`landmark_face.py`の`build_with_face_landmarks`**（旧実装）：いちごの写真で
  顔が検出されなかった（`frontal_min_neighbors=6`という厳しめの設定のため、
  `flat_style.py`の`minNeighbors=3`では検出できていた同じ顔を見逃した）。
  これは新旧2つの実装で意図的にパラメータが異なることによる既知の差であり、
  `flat_style.py`が「現在のメイン実装」である以上、優先して直す必要はないと
  判断（旧実装のパラメータを合わせる価値があるかは今後の判断）。
- **`paint_by_number.py`**（このセッションで初めてテスト）：いちごの写真・
  犬の写真ともにクラッシュなく、線画＋番号＋色凡例＋見本画像の3点セットが
  正常に出力されることを確認。CLAUDE.mdに記載の通りスタイルは`flat_style.py`
  移植前の古いものだが、機能自体は壊れていない。

## 2026-09-19 セッション（実写真バッチ4：5枚追加検証、コード変更なし）

ユーザーから追加で実写真5枚（遊具に乗る子供3人、ワンちゃんのライン画柄シャツで
変顔の男の子、鼻を指す男の子、柄物のアロハシャツでソファに立つ男の子、ハイチェアで
変顔の男の子＋犬）を受け取り検証。5枚とも致命的な破綻はなく、新規バグは
見つからなかった。**コードは変更していない。**

### 「多少の誤爆を許容する」判断が実写真で実際に可視化されたケース
ワンちゃんのライン画柄シャツの写真で、`recover_missed_people()`が背景の
遊具（オレンジ・黄色の膨らませた遊具）を横顔として誤検出し、本人の肩の横に
小さな顔つきの楕円形が浮かんで見える出力になった。バッチ1〜3で「本物の人物が
消えるより、多少の誤爆を受け入れる方を優先する」と決めた設計判断が、実際に
目に見える形で発現した初めてのケース。人物が消えるよりは実害が小さいという
判断は維持するが、「たまに浮遊する謎の顔が出る」という具体的な副作用が
実例として記録できた。

### 柄物の服のまだら化（優先順位3番、既知の課題）を実写真で再確認
同じ写真のシャツ（ワンちゃんのライン画柄）で、`flatten_busy_patterns()`後も
茶色っぽいまだら模様が残った。自作の高コントラストなチェッカー柄ほど酷くは
ないが、実際の柄物の服でも軽度に同じ傾向が出ることを確認。優先順位3番の
対応時に参考にする実例として記録。

### 個別には問題なかったもの
- 遊具に乗る子供3人の写真は、3人分の頭部が近接した状態で描かれ、破綻はなし。
- 鼻を指す男の子・アロハシャツの男の子・ハイチェアの男の子の3枚は、いずれも
  単純な1人構図で顔パーツも良好に描画された。ハイチェアの写真では手前の犬が
  小さな点状のノイズとして残ったが、視認性への影響はごく軽微。
- **回帰なし**：正面顔・横顔（合成テスト3種）、後ろ姿2種、犬単体、車単体、
  後ろ姿＋花柄シャツ、ドーナツ装飾＋父子（この写真は元々keep_maskが画面ほぼ全体を
  覆っていたため復元ロジック自体が発火せず、想定通り変化なし）は全て
  変更前と見た目が同一であることを確認。正面顔の写真1枚で新たに1件の復元が
  発火したが、出力画像を確認したところ既存の被写体と重なる範囲だったため
  目に見える悪影響はなかった。
